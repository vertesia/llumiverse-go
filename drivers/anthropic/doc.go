// Package anthropic exposes the direct Anthropic Claude Messages API driver.
// The implementation is shared with other Claude-hosting drivers through an
// internal Claude package so prompt, thinking, streaming, and conversation
// handling stay consistent.
package anthropic
